<% if resource.errors.any? %>
  <div id="error_explanation">
    <div class="text-center alert alert" style="margin-top:-55px;margin-bottom:25px ">
      <% resource.errors.full_messages.each do |message| %>
        <%= message + ".  "%>
      <% end %>
  </div>
  </div>
<% end %>
